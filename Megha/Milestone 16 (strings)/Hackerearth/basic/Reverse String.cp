

#include <iostream>
#include <string.h>
using namespace std;
int main() {
    int t,l;
    cin>>t;
    for(int i=0;i<t;i++){
    char s[1000001];
    cin>>s;
    int no=0,ans=0;
    for(l=strlen(s)-1;l>=0;l--){ 
        cout<<s[l];
    } 
    cout<<endl;
    }
    return 0;
}
