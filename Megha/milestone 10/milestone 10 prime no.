#include <iostream>
using namespace std;
int isprime(int n){
for(int j=2;j<=n;j++){
    int ans=1;
for(int i=2;i<j;i++){
    if(j%i==0){
       ans=0;
       break;
    }
}
if(ans==1){
    cout<<j<<",";
}
}
}
int main() {
  isprime(10);
    return 0;
}
