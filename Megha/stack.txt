#include <iostream>
#include<vector>
using namespace std;
classs stack
{
vector<int>v;
public:
void push(int data){
     v.push_back(data);
}
void pop(){
   if(v.empty()){
       cout<<"stack is underflow"<<endl;
       return -1;
   }
}
void topElement=top();
    return
}
}
int main() {
 
}
