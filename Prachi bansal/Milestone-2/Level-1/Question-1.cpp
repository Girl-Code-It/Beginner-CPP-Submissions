#include<iostream>
using namespace std;
int main(){
	int l, b;
		cout<<"Enter the length: ";
		cin>>l;
		cout<<"\nEnter the breadth: ";
		cin>>b;
		if(l==b)
		{cout<<"\nIt is a square";
		}else{
			cout<<"\nIt is not a square";
		}
		return 0;
}
